```react
  const [isOpen, setIsOpen] = useState({
    RewardList: false,
    PointList: false,
    PublishList: false,
    AdminList: false,
  });

  const toggleNav = e => {
    setIsOpen({ [e.target.name]: !isOpen[e.target.name] });
  };



//각각의 토글을 사용하고 싶을때 이리 한다. 컴퓨터가 알아볼 수 있도록, name 으로 들어갈 항목을 반드시 지정해준다.
```

